// niri config — host-specific part for IceCube. Lives next to this host's home
// config (homes/x86_64-linux/godtamit@IceCube/) and is deployed as
// ~/.config/niri/host.kdl by that default.nix. The generic config.kdl (in the
// shared user config) pulls it in via `include "host.kdl"`.
//
// This mirrors the host-specific Hyprland settings in this directory's
// default.nix (monitors, workspace pinning, app autostart, mouse sensitivity).
//
// Each host that enables niri must provide its own host.kdl — these
// outputs/workspaces are IceCube's. (config.kdl's `include "host.kdl"` is a hard
// requirement; niri errors if the included file is missing.)

// Dual 4K@144 displays
output "DP-5" {
    mode "3840x2160@144"
    scale 1.33
    position x=0 y=0
    variable-refresh-rate
}

output "DP-6" {
    mode "3840x2160@144"
    scale 1.33
    variable-refresh-rate
}

// Persistent, named workspaces pinned to outputs.
workspace "dev" { open-on-output "DP-5"; }
workspace "nix" { open-on-output "DP-5"; }
workspace "chat" { open-on-output "DP-5"; }
workspace "misc" { open-on-output "DP-5"; }
workspace "browser" { open-on-output "DP-6"; }
workspace "media" { open-on-output "DP-6"; }
workspace "gaming" { open-on-output "DP-6"; }

// Autostarted apps (Hyprland exec-once launched these with [workspace N silent];
// the open-on-workspace window rules in config.kdl route them onto the right
// workspaces).
spawn-at-startup "ferdium"
spawn-at-startup "signal-desktop"
spawn-at-startup "ytmdesktop"
spawn-at-startup "firefox"
spawn-at-startup "antigravity-ide"

// Workspace assignments
window-rule {
    match app-id=r#"(?i)ferdium"#
    match app-id=r#"(?i)signal"#
    open-on-workspace "chat"
    open-focused false
}
window-rule {
    match app-id=r#"(?i)youtube.?music"#
    open-on-workspace "media"
    open-focused false
}
window-rule {
    match at-startup=true app-id=r#"firefox$"#
    open-on-workspace "browser"
    open-focused false
}
window-rule {
    match at-startup=true app-id=r#"(?i)antigravity"#
    open-on-workspace "dev"
    open-focused false
}
window-rule {
    match app-id=r#"(?i)steam"#
    open-on-workspace "gaming"
    open-focused false
}

// ─── Permanent workspace keybinds ────────────────────────────────────────────
binds {
    // Focus a workspace by number (order matches the workspace declarations above).
    Mod+1 { focus-workspace "dev"; }
    Mod+2 { focus-workspace "nix"; }
    Mod+3 { focus-workspace "chat"; }
    Mod+4 { focus-workspace "misc"; }
    Mod+5 { focus-workspace "browser"; }
    Mod+6 { focus-workspace "media"; }
    Mod+7 { focus-workspace "gaming"; }

    // Move the focused column to a workspace.
    Mod+Shift+1 { move-column-to-workspace "dev"; }
    Mod+Shift+2 { move-column-to-workspace "nix"; }
    Mod+Shift+3 { move-column-to-workspace "chat"; }
    Mod+Shift+4 { move-column-to-workspace "misc"; }
    Mod+Shift+5 { move-column-to-workspace "browser"; }
    Mod+Shift+6 { move-column-to-workspace "media"; }
    Mod+Shift+7 { move-column-to-workspace "gaming"; }
}
